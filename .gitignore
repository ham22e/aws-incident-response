# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example

# Python
.venv/
__pycache__/
*.pyc

# Node / Next.js (app/webapp)
node_modules/
.next/
next-env.d.ts
npm-debug.log*
.env*.local
# archive_file이 생성하는 배포 zip
terraform/webapp.zip

# 수집한 원본 로그(민감 값 포함 가능) - 정제본만 커밋
analysis/raw/
# 정규화 산출물(공격자 IP/임시키 ID 포함 가능) - 손으로 정제/마스킹한 보고서만 커밋
analysis/normalized.csv
analysis/normalized.json

# 시크릿/자격증명
*.pem
*.key
aws-credentials*
stolen/
